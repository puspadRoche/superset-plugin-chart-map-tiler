{
  "name": "superset-plugin-chart-map-tiler",
  "version": "0.1.0",
  "description": "MapLibre + MapTiler visualization plugin for Apache Superset 3.1.0 (fake-data fallback).",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -w -p tsconfig.json",
    "prepublishOnly": "npm run build"
  },
  "peerDependencies": {
    "@superset-ui/chart-controls": "^3",
    "@superset-ui/core": "^3",
    "react": "^18",
    "react-dom": "^18"
  },
  "dependencies": {
    "maplibre-gl": "^3.6.0"
  },
  "keywords": [
    "apache-superset",
    "superset",
    "maplibre",
    "maptiler",
    "visualization-plugin"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.0",
    "typescript": "^5.6.3"
  }
}
